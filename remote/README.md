# Remote Setup

*This is a proof of concept. The config parameters are not optimized.*

## Setup BuildFarm

1. Copy `remote/buildfarm` to server.

2. Enter `buildfarm` folder.

3. Run `./build-worker.sh`.

4. Run `podman compose up -d`.

That's all.
